/**
 * InteractiveObject
 * The InteractiveObject class is the abstract base class
 * for all display objects with which the user can interact,
 * using the mouse, keyboard, or other user input device.
 * @package aj.display
 */
define(["aj/display/DisplayObject"], function(DisplayObject)
{
   return new JS.Class(DisplayObject, {
   });
});