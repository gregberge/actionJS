/**
 * DisplayObjectContainer
 * The DisplayObjectContainer class is the base class
 * for all objects that can serve as display object containers
 * on the display list.
 * @package aj.display
 */
define(["aj/display/InteractiveObject"], function(InteractiveObject)
{
   return new JS.Class(InteractiveObject, {
   });
});